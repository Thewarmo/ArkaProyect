# Test configuration for Config Server

# Use native profile for tests (local filesystem)
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=classpath:/config

# Server port for tests
server.port=8888
